# Optional geospatial stack for the full Sentinel-2 workflow.
# Install only after the quickstart pipeline works.
pystac-client>=0.7
planetary-computer>=1.0
rasterio>=1.3
rioxarray>=0.15
xarray>=2024.1
stackstac>=0.5
tqdm>=4.66
geopandas>=0.14
shapely>=2.0
